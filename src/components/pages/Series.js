import React, { Component } from 'react';
import  {
    View,
    Text,
    Alert,
    TextInput,
    Image,
    TouchableHighlight,
    StyleSheet
} from 'react-native';



const Series = React.createClass({
    getInitialState() {
        return {

        }
    },

    render () {
        return (
            <Text>Series</Text>
        );
    }
});

export default Series;
