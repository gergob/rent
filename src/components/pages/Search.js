import React, { Component } from 'react';
import  {
    View,
    Text,
    Alert,
    TextInput,
    Image,
    TouchableHighlight,
    StyleSheet
} from 'react-native';



const Search = React.createClass({
    getInitialState() {
        return {

        }
    },

    render () {
        return (
            <Text>Search</Text>
        );
    }
});

export default Search;
