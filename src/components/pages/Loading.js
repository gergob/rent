import React, { Component } from 'react';
import  {
    View,
    Text,
    Alert,
    TextInput,
    Image,
    TouchableHighlight,
    StyleSheet
} from 'react-native';



const Loading = React.createClass({
    getInitialState() {
        return {

        }
    },

    render () {
        return (
            <Text>Loading</Text>
        );
    }
});

export default Loading;
